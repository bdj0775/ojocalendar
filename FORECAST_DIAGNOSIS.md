# FORECAST_DIAGNOSIS.md — 예상 점유율 100% 문제 진단 보고서

> **[2026-09-06 갱신] 이 문서는 진단 당시의 기록입니다.**
> 이후 headroom을 제거해 조치를 완료했고, 시스템 전반 설명은 [FORECAST_SYSTEM.md](./FORECAST_SYSTEM.md)로 옮겼습니다.
> 아래 3장의 "비수기와 성수기를 구분하지 못함" 지적은 **철회합니다** — 2027년 1·2월에 80~95%를
> 예측하는 것이 과하다고 판단했으나, 실제로 그 시기에도 74%·89%를 기록한 이력이 있어 근거가 없었습니다.
> 데이터가 아닌 계절 통념으로 내린 잘못된 판단이었습니다.


> 작성일: 2026-09-06
> 상태: **조치 완료** (headroom 제거 — 커밋 a38ce5e)
> 검증 도구: `node --import ./__loader-reg.mjs run_hook_check.mjs`

---

## 0. 먼저 — 이전 보고가 왜 틀렸나

두 번에 걸쳐 "고쳤다"고 보고했으나 화면은 그대로 100%였습니다. 원인은 **검증 방법**에 있었습니다.

앱 코드를 읽고 **비슷하게 다시 짠 스크립트**로 검증했는데, 그 스크립트가 앱과 달랐습니다.
결정적 차이는 편향(bias) 계산에 쓰이는 **샘플 선정 조건**이었습니다.

| | 내 스크립트 | 실제 앱 |
|---|---|---|
| 샘플 조건 | 대상월 3건 **+ 작년 같은달도 3건** | 대상월 3건만 |
| 실제 샘플 수 | **3개** | **11개** |
| 그 결과 내린 결론 | "샘플이 3개뿐이라 편향이 폭주" | **오진** |

이 오진 위에서 `biasClampFor()`(샘플 4개 미만이면 ±2%p)를 만들었는데, 실제 샘플은 11개라
이 장치가 아무 일도 하지 않았습니다. 그래서 화면이 바뀌지 않았습니다.

**교훈:** 앞으로 이 훅을 검증할 때는 반드시 `run_hook_check.mjs`를 씁니다. 이 스크립트는
`useDesktopStats.ts`를 **직접 import**하고 react/store만 가짜로 채우므로 화면 값과 일치합니다.
실제로 이것으로 화면의 100%·신뢰도 67%(캡처 68%)를 재현해 원인을 특정했습니다.

---

## 1. 지금 화면에 무엇이 나오는가 (실측)

`run_hook_check.mjs` 실행 결과 — 캡처와 일치합니다.

```
월          점유율   예상점유율   신뢰도
2026 9월       77%        91%      60%  ← 이번달
2026 10월      74%       100%      67%
2026 11월      70%       100%      50%
2026 12월      35%        92%      41%
2027 1월        6%        80%      36%
2027 2월        4%        95%      26%
```

내부 값:

```
2026-10: D=25  OTB=74%  raw(편향전)=92%  bias=+10  → 102% → 100%
2026-11: D=56  OTB=70%  raw=93%          bias=+10  → 103% → 100%
2026-12: D=86  OTB=35%  raw=82%          bias=+10  →  92%
τ(추정) = 52.0
```

---

## 2. 진짜 원인 — 두 힘이 서로를 상쇄하고 있다

### 2-1. 모델은 일관되게 **과소예측**한다

편향 계산에 쓰이는 11개 달 전부에서 오차가 **양수**입니다(실제 > 예측).

| 달 | D-25 OTB | D-25 예측 | 실제 | 오차 |
|---|---|---|---|---|
| 2026-08 | 65% | 79% | 100% | **+21** |
| 2026-07 | 65% | 79% | 90% | +11 |
| 2026-06 | 60% | 74% | 93% | +19 |
| 2026-05 | 68% | 81% | 100% | +19 |
| 2026-04 | 53% | 68% | 93% | **+25** |
| 2026-03 | 55% | 70% | 87% | +17 |
| 2026-02 | 32% | 50% | 89% | **+39** |
| 2026-01 | 13% | 32% | 74% | **+42** |
| 2025-12 | 32% | 48% | 71% | +23 |
| 2025-11 | 43% | 58% | 87% | +29 |
| 2025-10 | 84% | 93% | 94% | +1 |

**평균 편향 +22.4%p.** 편향 보정은 이 과소예측을 되돌리려는 **정상 동작**입니다.

### 2-2. 과소예측의 범인은 내가 넣은 `headroom`이다

이번에 "만실 근처에서 덜 팔린다"는 취지로 잔여 픽업에 `headroom = (100-OTB)/100`을
곱했습니다. 그런데 이게 픽업을 과도하게 깎습니다.

```
실측(11개 달 평균): D-25 시점 OTB 52% → 최종 89%   = 실제 픽업 37%p

모델의 예상 픽업:
  histAvgOcc(90) × (1-cc)(0.382) × headroom(0.48) = 17%p
                                    ↑ 이 곱셈이 절반 이상을 깎아냄
```

**17%p를 예상하는데 실제로는 37%p가 들어옵니다.** 그래서 예측이 낮게 나오고,
편향 보정이 +10%p(상한)까지 되돌리고, 결과적으로 100%가 됩니다.

### 2-3. 즉 현재 상태는

```
headroom이 과하게 깎음  →  예측 낮아짐  →  편향이 +22%p로 커짐
                                              ↓
                              상한 ±10%p까지 되돌림  →  다시 100%
```

**두 장치가 서로 싸우고 있습니다.** 그래서 겉보기 결과가 수정 전과 같습니다.

---

## 3. 중요한 사실 — 100%가 틀린 예측이 아닐 수 있다

운영자 지적대로, 이 숙소는 **실제로 만실이 잦습니다**.

```
2025-10  94%     2026-04  93%     2026-08 100%
2025-11  87%     2026-05 100%
2026-02  89%     2026-06  93%
2026-03  87%     2026-07  90%
```

11개 달 중 **평균 실제 점유율이 89%**입니다. D-25에 OTB 74%인 10월이 최종 90~100%로
끝나는 것은 이 데이터상 **매우 합리적인 예측**입니다.

**따라서 "100%가 나오는 것" 자체는 버그가 아닙니다.** 진짜 문제는 다음입니다.

1. **비수기와 성수기를 구분하지 못함** — 2027년 1·2월(OTB 6%, 4%)에도 80%, 95%를
   예측합니다. 이건 명백히 과합니다.
2. **먼 미래에도 같은 확신** — 2027-02는 D=90 이상인데 95%를 냅니다.
3. **두 장치가 상쇄되어 조정이 불가능한 상태** — headroom을 조정해도 편향이 반대로
   움직여 상쇄합니다.

---

## 4. 수정 방향 (결정 필요)

### 방향 A — headroom을 빼고 편향에 맡긴다 (권장)

`headroom` 곱셈을 제거해 픽업을 실측에 맞추면, 편향이 자연히 작아집니다.
두 장치가 싸우지 않게 되어 이후 조정이 가능해집니다.

- 장점: 구조가 단순해지고 편향 보정이 제 역할을 함
- 단점: 만실 근처 억제 효과가 사라짐 (다만 `min(100)`이 있어 폭주하진 않음)

### 방향 B — headroom을 약하게 (제곱근 등)

`headroom` 대신 `sqrt(headroom)`처럼 완만하게 적용합니다.
OTB 52%일 때 0.48 → 0.69로 덜 깎습니다.

- 장점: 만실 억제 취지를 살리면서 과소예측 완화
- 단점: 임의 조정값이라 근거가 약함

### 방향 C — 비수기 판별을 별도로 넣는다

지금 문제의 본질은 "10월 100%"가 아니라 **"2027년 1·2월도 80~95%"** 입니다.
STLY가 낮은 달(작년 1월 74%, 2월 89%)인데도 높게 나오는 건 편향 +10%p가
계절 구분 없이 일괄 적용되기 때문입니다.

→ 편향을 **월별/계절별로 나눠** 계산하면 근본 해결에 가깝습니다.
   다만 샘플이 달당 1개뿐이라 지금 데이터로는 어렵습니다.

**권장: A를 먼저 적용해 상쇄 구조를 없애고, 그다음 실측으로 재평가.**

---

## 5. 지금 코드 상태

| 커밋 | 내용 | 상태 |
|---|---|---|
| `43fd02e` | 점유율을 객실박 기준으로 통일 | ✅ 유효 (1객실이라 수치 변화 없음) |
| `661bd01` | headroom + 동적가중 + 먼미래 신뢰도 하향 | ⚠️ headroom이 과소예측 유발 |
| `d0912a5` | 편향 인라인 공식 통일 + D 범위 확대 + 샘플별 상한 | ⚠️ `biasClampFor`는 오진 기반, 사실상 무효 |
| (최신) | 검증 스크립트를 실제 훅 실행 방식으로 교체 | ✅ |

**유효한 개선:**
- 편향 계산이 옛 공식을 쓰던 버그 수정 (실제 버그였음)
- 편향 D 범위를 달 진행 중(D=-31)까지 확대 (실제 버그였음)
- 먼 미래 신뢰도 하향 — 2027-02가 26%로 표시됨 (의도대로 작동)
- 동적 가중치

**되돌리거나 재검토할 것:**
- `headroom` 곱셈 (과소예측 유발)
- `biasClampFor` (오진 기반. 샘플은 11개이므로 이 함수는 사실상 ±10 고정)

---

## 6. 다음 세션에서 할 일

1. `node --import ./__loader-reg.mjs run_hook_check.mjs` 로 현재 값 확인
2. 방향 A/B/C 중 결정
3. 수정 후 **반드시 같은 스크립트로 재확인** (별도 재현 스크립트 금지)
4. `node backtest_v2.mjs` 로 정확도 회귀 확인

### 참고: 검증 스크립트 사용법

```bash
# 앱 실제 훅 실행 (화면과 동일한 값)
node --import ./__loader-reg.mjs run_hook_check.mjs

# 다른 날짜 기준으로 보기
FAKE_TODAY=2026-10-15 node --import ./__loader-reg.mjs run_hook_check.mjs
```

`__loader.mjs`, `__loader-reg.mjs`, `__fake_*.mjs`는 gitignore 대상이며
없으면 `run_hook_check.mjs` 상단 주석을 참고해 다시 만들면 됩니다.

---

## 7. 별건 — 미해결 항목

- **iCal "Not available" 블록**: 182박짜리는 운영자가 직접 삭제함. 앞으로 자동 유입을
  막을지(`syncService.ts` 수정 — CLAUDE.md 레드라인) 논의 필요.
  단, `2026-10-18~20` 2박 48.5만원 건은 **이름만 마스킹된 실제 예약**이므로 지우면 안 됨.
- **관리자 탭 미표시**: 배포본에 반영 안 된 상태로 확인 중단됨. 로컬에서 재확인 필요.
- **push 안 한 커밋 다수**: 예측 관련 커밋들이 로컬에만 있음.
