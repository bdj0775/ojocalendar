# FORECAST_SYSTEM.md — 점유율 예측 시스템 인수인계 문서

> 최종 갱신: 2026-09-06 (알고리즘 전면 교체 — "픽업6")
> 대상: 이 시스템을 처음 보는 사람 (개발자 / 운영자 본인 / 외주)
> 관련 파일: [`src/hooks/useDesktopStats.ts`](src/hooks/useDesktopStats.ts) — 예측 로직 전부가 이 파일 하나에 있습니다.

---

## 0. 3분 요약

대시보드의 **"월별 추이 (11개월)"** 카드에 점선으로 그려지는 **예상 점유율**을 만드는 시스템입니다.

- **하는 일**: "이번 달(또는 다음 달)이 최종적으로 몇 % 찰까"를 예측
- **핵심 아이디어 (픽업6)**: 지금 확정된 예약(OTB)에, **최근 달들이 같은 시점에서
  월말까지 추가로 채운 폭(잔여픽업)의 평균**을 더한다. 그게 전부다.
- **현재 정확도**: 평균 오차 **8.0%p** (교체 전 12.8%p — 모든 시점 구간에서 개선)
- **정확도 확인 방법**: `node --import ./__loader-reg.mjs monthly_review.mjs`

**⚠️ 가장 중요한 규칙: 이 로직을 수정했다면 반드시 실제 훅으로 검증하세요.**
공식을 따로 재현한 스크립트로 검증하다가 실제와 다른 결론을 내려 두 번 잘못 고친 전례가 있습니다
(경위: [FORECAST_DIAGNOSIS.md](./FORECAST_DIAGNOSIS.md)).

> 📌 2026-09-06 이전에는 "페이스 예측 + STLY 동적가중 + 편향보정 커브"라는 복잡한
> 방식을 썼습니다. 실데이터 301건 백테스트에서 후보 20여 개와 비교한 결과, 훨씬
> 단순한 픽업6이 가장 정확해 전면 교체했습니다. 교체 근거와 실험 전체 기록:
> [FORECAST_REBUILD_2026-09.md](./FORECAST_REBUILD_2026-09.md)

---

## 1. 용어 (이것만 알면 나머지가 읽힙니다)

| 용어 | 뜻 | 예시 |
|---|---|---|
| **점유율(Occupancy)** | 판매된 객실박 ÷ 판매 가능한 객실박 | 30일 중 23박 팔림 → 77% |
| **OTB** (On The Books) | **지금까지 확정된** 예약만으로 계산한 점유율 | 10월이 아직 안 왔는데 이미 74% 예약됨 |
| **잔여픽업(Pickup)** | 어떤 시점의 OTB에서 월말 최종까지 추가로 채워진 폭 | 이맘때 60% → 최종 93%면 픽업 33%p |
| **D** | 그 달 **시작까지 남은 일수** | 9월 6일에 10월을 보면 D=25 |
| **같은 시점** | 히스토리 달의 시작일 기준 같은 D(또는 같은 경과일) | 10월을 D-25에 보면, 8월의 "같은 시점"은 7월 7일 |
| **STLY** (Same Time Last Year) | 작년 같은 달 실적. **계산엔 안 쓰고** 팝오버 참고 표시용 | 2025년 10월 = 94% |
| **τ (타우)** | 예약 도착 속도 상수. **신뢰도 표시에만** 사용 | 현재 자동 추정값 약 52 |
| **리드타임** | 예약이 체크인 며칠 전에 들어왔는지 | 중앙값 31일 |

---

## 2. 예측이 어떻게 만들어지는가 — 픽업6

### 공식 (이게 전부입니다)

```
예상 점유율 = 현재 OTB + mean( 최종점유율_h − 같은시점OTB_h )

  h = 대상월 직전 6개 달력월 중 "완료됐고 예약 ≥3건"인 달
```

여기에 상식적인 클램프 세 개가 붙습니다.

1. **바닥**: 예상치는 현재 OTB보다 낮을 수 없다 (이미 확정된 예약은 사라지지 않음)
2. **천장**: 100%를 넘을 수 없다
3. **물리적 상한** (진행 중인 달만): 이미 지나간 공실 날짜는 되돌릴 수 없으므로
   `(지나간 날 중 점유된 박 + 남은 일수) ÷ 월 일수`를 상한으로 쓴다

### 예시 (2026-09-06에 10월을 예측, D-25 — 실데이터 그대로)

| 히스토리 달 | D-25 시점 OTB | 최종 | 잔여픽업 |
|---|---|---|---|
| 2026-04 | 57% | 93% | +36 |
| 2026-05 | 68% | 100% | +32 |
| 2026-06 | 60% | 93% | +33 |
| 2026-07 | 65% | 90% | +25 |
| 2026-08 | 65% | 100% | +35 |
| (2026-09) | — 아직 미완료라 제외 | | |
| **평균** | | | **+32** |

10월 현재 OTB 74% + 32%p = **106% → 클램프 → 100%**

### 세부 규칙

- **히스토리 달의 "같은 시점 OTB" 재현**: 그 달 시작 D일 전(진행 중이면 경과 e일 후)
  까지 **접수된(bookingDate)** 예약만으로 점유율을 다시 계산합니다.
  접수일이 없거나 체크인보다 늦게 기록된 이상치는 체크인일로 간주합니다(안전한 하한).
- **월중 시점**: 히스토리 달이 그 경과일에 이미 끝났으면(예: 31일차 vs 2월) 픽업 0으로 칩니다.
- **윈도가 "대상월 직전 6개 달력월 ∩ 완료된 달"인 이유**: 먼 미래 달일수록 표본이
  3~4개로 줄어드는 구조인데, 백테스트에서 "무조건 최근 완료 6개달"보다 이 방식이
  먼 지평(D-60~120)에서 명확히 정확했습니다 (MAE 22.1 → 17.7%p). **바꾸지 마세요.**
- **폴백** (완료된 히스토리가 하나도 없는 오픈 직후):
  `OTB + 최근평균최종점유율 × (1 − 진행률)`, 진행률은 `exp(−D/τ)` 또는 경과일 비율.
- **STLY·편향보정·동적가중은 더 이상 쓰지 않습니다.** 백테스트에서 전부 픽업6보다
  부정확했습니다. 특히 "공식이 두 곳에 복사되어 있다"는 옛 규칙은 편향보정 커브가
  사라지면서 함께 소멸했습니다 — **이제 예측 공식은 `computeForecast()` 한 곳뿐입니다.**

### 팝오버와의 관계

차트의 ⓘ 팝오버(`ForecastExplainer.tsx`)는 이 계산을 그대로 보여줍니다:
"지금 {OTB}% + 최근 {N}개 달 평균 픽업 {P}%p = {예상}%".
`MonthlyTrend.expectedPickup`·`histMonthsUsed` 필드가 이를 위해 존재합니다.
**공식을 바꾸면 팝오버 문구도 함께 바꿔야 합니다.**

---

## 3. 신뢰도 표시

각 예측에는 **신뢰도(0~100%)** 가 함께 계산되어 차트 툴팁에 표시됩니다.

```
신뢰도 = 0.4 × 데이터충실도 + 0.6 × 시간근접도
  데이터충실도 = min(1, 히스토리 달 수 / 4)
  시간근접도  = exp(−D / τ)
  └ D가 120일을 넘으면 추가로 지수 감쇠
```

**왜 120일인가**: 실측한 예약 리드타임이 근거입니다.

| 지표 | 값 |
|---|---|
| 리드타임 중앙값 | 31일 |
| D-90 이전에 접수되는 비율 | 17% |
| D-180 이전 | 3% |

즉 D-120을 넘는 달은 "아직 안 팔린" 게 아니라 **"팔릴 시기가 오지 않은"** 것이라
판단 근거 자체가 없습니다. 신뢰도가 **30% 미만**이면 툴팁에 주황색으로
*"아직 예약이 들어올 시기가 아니라 참고용입니다"* 가 표시됩니다.

---

## 4. 현재 정확도 (2026-09-06 실측, 완료된 15개 달)

`backtest_hook.mjs` — 실제 훅을 과거 시점으로 되돌려 실행한 결과입니다.
괄호 안은 교체 전 구방식.

| 시점 | MAE | 편향 |
|---|---|---|
| D-90 | 17.0%p (27.2) | -11.1%p |
| D-60 | 15.5%p (25.8) | -9.6%p |
| D-30 | 9.7%p (15.1) | -5.7%p |
| D-14 | 8.9%p (14.1) | -4.6%p |
| D-7 | 8.2%p (13.9) | -4.2%p |
| 1일차 | 9.3%p (13.3) | +2.5%p |
| 10일차 | 6.6%p (10.2) | +1.5%p |
| 15일차 | 4.4%p (8.1) | +1.2%p |
| 20일차 | 2.8%p (5.5) | +0.1%p |
| 30일차 | 1.0%p (1.2) | -1.0%p |
| **전체** | **8.0%p (12.8)** | **-2.4%p (-2.8)** |

**최근 6개월 상세** (`monthly_review.mjs`):

| 월 | 실제 | D-30 | D-7 | 1일차 | 15일차 |
|---|---|---|---|---|---|
| 2026-03 | 87% | 81%(-6) | 87%(+0) | 89%(+2) | 88%(+1) |
| 2026-04 | 93% | 89%(-4) | 93%(+0) | 89%(-4) | 91%(-2) |
| 2026-05 | 100% | 100%(+0) | 100%(+0) | 100%(+0) | 100%(+0) |
| 2026-06 | 93% | 100%(+7) | 100%(+7) | 100%(+7) | 93%(+0) |
| 2026-07 | 90% | 100%(+10) | 100%(+10) | 97%(+7) | 90%(+0) |
| 2026-08 | 100% | 100%(+0) | 100%(+0) | 100%(+0) | 100%(+0) |

### 알려진 특성

1. **오차의 대부분은 오픈 초기(2025-06~11)에서 나옵니다** — 그때는 참조할 히스토리가
   없었기 때문으로, 지금 시점의 예측 품질과는 무관합니다. 2026-03 이후 완료달들의
   일단위 전체 오차는 월평균 1.9~7.7%p입니다.
2. **거의 만실인 달(93~100%)을 100%로 예측하는 경향** — 이 숙소는 실제로 만실이
   잦아 100% 예측 자체는 오류가 아니며, 빗나가도 3~10%p 이내입니다.
3. **먼 미래(D-90+)는 구조적 한계** — 예약이 아직 안 들어온 시기라 어떤 방법으로도
   크게 못 줄입니다. 신뢰도 표시로 보완합니다.
4. **겨울 달(12~1월)의 먼 지평 예측** — 픽업6은 계절을 모르므로, 여름 픽업으로 겨울을
   예측하면 먼 지평에서 과대예측 소지가 있습니다. STLY가 2년째부터 쌓이므로
   "D-60 이상에서 STLY 블렌드"를 후보로 남겨뒀지만, 아직 백테스트로 검증할 표본이
   없어 넣지 않았습니다(6장 참고).

---

## 5. 월말 점검 절차 ⭐

매월 말 또는 새 달이 완료될 때마다 실행하면, 예측이 실제와 얼마나 맞았는지 확인하고
정확도가 나아지는지 추적할 수 있습니다.

### 5-1. 데이터 내려받기

Supabase 대시보드 → SQL Editor에서 실행합니다.
**결과가 100줄로 잘리므로 `OFFSET`을 바꿔가며 여러 번 받아야 합니다.**

```sql
-- 전체 건수 먼저 확인
SELECT COUNT(*) FROM bookings
WHERE status IN ('confirmed','checked in','completed');

-- 100건씩 받기 (OFFSET을 0, 100, 200, 300... 으로 바꿔가며 반복)
SELECT guestname, checkin, checkout, bookingdate,
       amount, commission, channel, status, property_id
FROM bookings
WHERE status IN ('confirmed','checked in','completed')
ORDER BY checkin LIMIT 100 OFFSET 0;
```

각 결과를 **Export → Download CSV** 하고, 프로젝트 폴더에 모아둡니다.

### 5-2. 파일 합치기

받은 CSV들을 `bookings_latest.csv` 하나로 합칩니다. 파일명이
`Supabase Snippet Untitled query (1).csv` 형태라면:

```bash
node -e "
const fs=require('fs');let all=[],head=null;
for(const i of [1,2,3,4]){
  const t=fs.readFileSync('Supabase Snippet Untitled query ('+i+').csv','utf8').replace(/^﻿/,'').trim();
  const L=t.split(/\r?\n/); if(!head)head=L[0]; all=all.concat(L.slice(1));
}
fs.writeFileSync('bookings_latest.csv',head+'\n'+all.join('\n'));
console.log('합침:',all.length,'건');
"
```

### 5-3. 점검 실행

```bash
node --import ./__loader-reg.mjs monthly_review.mjs
```

출력에 나오는 **전체 MAE와 편향**을 아래 이력 표에 기록하세요.

> `__loader-reg.mjs` 등이 없다면 7장 "검증 도구" 참고해서 다시 만들면 됩니다.

### 5-4. 정확도 이력

| 점검일 | 완료 달 수 | 전체 MAE | 편향 | 비고 |
|---|---|---|---|---|
| 2026-09-06 | 15개 | 14.0%p | -4.2%p | (구방식) headroom 제거, 동적가중 도입 후 |
| 2026-09-06 | 15개 | **8.7%p** | -2.8%p | **픽업6 교체 직후** (monthly_review 기준) |
| | | | | |

**추세 읽는 법**
- MAE가 **줄어들면** 데이터가 쌓이며 예측이 나아지는 것 — 정상
- MAE가 **늘어나면** 영업 패턴이 바뀌었거나 로직 수정이 역효과 — 조사 필요
- 편향이 한쪽으로 **커지면** 아래 조정 가이드 참고

---

## 6. 조정 가이드 — 정확도를 더 높이려면

### 먼저: 조정하기 전에 읽을 것

**한 번에 하나만 바꾸고, 매번 백테스트로 확인하세요.**

```bash
# 바꾸기 전 수치 기록
node --import ./__loader-reg.mjs backtest_hook.mjs
# 코드 수정
# 바꾼 뒤 수치 비교
node --import ./__loader-reg.mjs backtest_hook.mjs
```

### 조정 지점 (전부 `useDesktopStats.ts`)

| 상수/코드 | 현재값 | 의미 |
|---|---|---|
| `PICKUP_WINDOW_MONTHS` | 6 | 픽업 평균에 쓸 직전 달력월 수. 5~7도 근소 차이(백테스트 완료) |
| `MIN_RELIABLE_BOOKINGS` | 3 | 이 미만 예약인 달은 히스토리에서 제외 |
| `FORECAST_RELIABLE_HORIZON_DAYS` | 120 | 이 일수 넘으면 신뢰도 급감 |
| `LOW_CONFIDENCE_THRESHOLD` | 0.3 | 이 미만이면 UI에 "참고용" 표시 |

### 이미 시험해서 기각된 것들 (다시 시도하기 전에 읽을 것)

2026-09 백테스트에서 아래 후보들은 모두 픽업6과 같거나 더 부정확했습니다.
상세 수치: [FORECAST_REBUILD_2026-09.md](./FORECAST_REBUILD_2026-09.md)

| 후보 | 결과 |
|---|---|
| 곱셈 픽업 (OTB ÷ 과거 같은시점 비율) | OTB가 작을 때 폭주. MAE 9.8%p |
| 중앙값 픽업 | 평균과 사실상 동률 (±0.1%p) |
| STLY/최근평균 앵커 블렌드 | 개선 없음 |
| 지수감쇠 가중평균 | 단순 평균보다 못함 |
| OTB 편차 감쇠(페이스 보정 β) | 음수 β 소폭, 양수 β 악화 — 복잡도 대비 무가치 |
| headroom `(100−OTB)/100` | 구방식에서 이미 기각 (과소예측 유발) |
| "최근 완료 6개달" 윈도 | 먼 지평 크게 악화 (22.1 vs 17.7%p) |

### 앞으로 시도할 가치가 있는 것

- **D-60 이상에서 STLY 블렌드**: 계절성 반영. 2026-10부터 "완료달 + 작년 실적" 쌍이
  생기므로 그때 백테스트로 검증 가능. 그 전엔 넣지 말 것(검증 불가).

---

## 7. 검증 도구

### `run_hook_check.mjs` — 지금 화면에 뭐가 나오는지 확인

```bash
node --import ./__loader-reg.mjs run_hook_check.mjs
# 특정 날짜 기준으로 보기
FAKE_TODAY=2026-10-15 node --import ./__loader-reg.mjs run_hook_check.mjs
```

### `backtest_hook.mjs` — 과거 전 구간 정확도 측정

```bash
node --import ./__loader-reg.mjs backtest_hook.mjs
```

D-90부터 30일차까지 모든 시점을 검증합니다. 로직 수정 전후 비교용.

### `monthly_review.mjs` — 월말 정기 점검 (5장 참고)

### ⚠️ 이 도구들이 특별한 이유

세 스크립트 모두 **`useDesktopStats.ts`를 직접 import**하고 React/스토어만 가짜로 채웁니다.
공식을 따로 베껴 쓰지 않으므로 **화면 값과 반드시 일치합니다.**

과거에 공식을 재현한 별도 스크립트로 검증하다가, 편향 샘플 조건이 미묘하게 달라
샘플 수를 3개로 오인(실제 11개)하고 잘못된 수정을 두 번 했습니다.
**앞으로도 최종 검증은 반드시 실제 훅으로 하세요.**
(새 알고리즘을 설계할 때 후보 비교용 프로토타입을 쓰는 것은 괜찮지만,
채택 판단과 최종 수치는 훅 기반 스크립트로 확인해야 합니다.)

### 보조 파일 (gitignore 대상, 없으면 다시 만들 것)

```bash
cat > __loader.mjs <<'EOF'
export async function resolve(specifier, context, next) {
  if (specifier === 'react') return { url: new URL('./__fake_react.mjs', import.meta.url).href, shortCircuit: true };
  if (specifier.endsWith('store/useStore')) return { url: new URL('./__fake_store.mjs', import.meta.url).href, shortCircuit: true };
  if (specifier.endsWith('utils/colors')) return { url: new URL('./__fake_colors.mjs', import.meta.url).href, shortCircuit: true };
  return next(specifier, context);
}
EOF
cat > __loader-reg.mjs <<'EOF'
import { register } from 'node:module';
register('./__loader.mjs', import.meta.url);
EOF
cat > __fake_react.mjs <<'EOF'
export const useMemo = (fn) => fn();
EOF
cat > __fake_store.mjs <<'EOF'
export const useStore = () => globalThis.__STORE__;
useStore.getState = () => globalThis.__STORE__;
EOF
cat > __fake_colors.mjs <<'EOF'
export const getNatColor = () => '#888888';
EOF
```

---

## 8. 데이터 품질 주의사항

예측은 데이터 품질에 직결됩니다. 다음을 주기적으로 확인하세요.

### iCal "Not available" 항목

Booking.com이 게스트 이름을 안 넘기면 `Not available`로 들어옵니다. **두 종류가 섞여 있습니다.**

| 종류 | 판별 | 처리 |
|---|---|---|
| 실제 예약 (이름만 마스킹) | 금액 있음, 보통 며칠 | **그대로 두기** |
| 판매 차단 블록 | **금액 0원**, 장기간 | **삭제** — 예약이 아닌데 점유율을 100%로 만듦 |

과거에 `2027-08-31 ~ 2028-02-29` 182박 0원짜리가 있어 그 기간 점유율이 전부 100%로
계산됐습니다(운영자가 직접 삭제함). 검증 스크립트들은 이 조건으로 자동 제외하지만,
**앱 화면은 제외하지 않습니다.** 발견하면 지우세요.

```sql
-- 의심 항목 찾기
SELECT guestname, checkin, checkout, amount, channel,
       (checkout::date - checkin::date) AS 박수
FROM bookings
WHERE guestname = 'Not available' OR (checkout::date - checkin::date) >= 7
ORDER BY 박수 DESC;
```

### 접수일(bookingdate) 이상치

접수일이 미래이거나 체크인보다 늦으면 히스토리 OTB 재현이 왜곡됩니다.
**픽업6은 접수일 > 체크인인 예약을 체크인일로 클램프**해 1차 방어하지만
(예: `김호현` 2025-12 체크인에 접수일 2027-12), 데이터 자체를 고치는 게 좋습니다.

```sql
SELECT guestname, checkin, bookingdate FROM bookings
WHERE bookingdate > checkin OR bookingdate > CURRENT_DATE
ORDER BY bookingdate DESC;
```

---

## 9. 이 시스템을 건드릴 때 지킬 것

1. **최종 검증은 실제 훅으로** — 재현 스크립트로 채택 판단 금지 (7장).
2. **한 번에 하나씩** — 동시에 여러 개 바꾸면 상쇄되어 효과 판별 불가.
3. **100%가 나온다고 버그가 아님** — 이 숙소는 실제로 만실이 잦습니다. 실제 데이터와
   비교해서 판단하세요. 성수기/비수기 통념으로 판단하지 말 것.
4. **윈도 정의를 바꾸지 말 것** — "대상월 직전 6개 달력월 ∩ 완료된 달"이 백테스트 승자.
5. **팝오버 동기화** — 공식을 바꾸면 `ForecastExplainer.tsx` 문구도 함께.
6. **기각된 후보를 다시 넣기 전에 6장 표를 읽을 것.**
7. **[FORECAST_ALGORITHM.md](./FORECAST_ALGORITHM.md)·[FORECAST_DIAGNOSIS.md](./FORECAST_DIAGNOSIS.md)** 는
   2026-09 교체 이전 구방식의 기록입니다. 현재 코드와 다르니 이 문서를 우선하세요.

---

## 10. 관련 문서

- [FORECAST_REBUILD_2026-09.md](./FORECAST_REBUILD_2026-09.md) — 픽업6 채택 근거: 후보 비교 백테스트 전체 기록
- [FORECAST_DIAGNOSIS.md](./FORECAST_DIAGNOSIS.md) — (구방식) 2026-09 "100% 고정" 문제 진단 기록
- [FORECAST_ALGORITHM.md](./FORECAST_ALGORITHM.md) — (구방식) 초기 알고리즘 설명서
- [CLAUDE.md](./CLAUDE.md) — 프로젝트 전체 코딩 규칙, 레드라인
- [PROJECT_STATUS.md](./PROJECT_STATUS.md) — 프로젝트 전반 현황
