# FORECAST.md — 예상 점유율 시스템 (단일 문서)

> 최종 갱신: 2026-09-06 · 예측 관련 문서는 이 파일 하나입니다.
> 코드: [`src/hooks/useDesktopStats.ts`](src/hooks/useDesktopStats.ts)의 `computeForecast()` 한 곳.
> 이전 문서들(FORECAST_SYSTEM/ALGORITHM/DIAGNOSIS/REBUILD)은 여기로 통합·삭제됨 — 원문은 git 히스토리(커밋 e458944 이전) 참조.

---

## 1. 무엇을 하는가

대시보드 **"월별 추이"** 카드의 점선(예상 점유율)을 만듭니다.
"이번 달(또는 다음 달)이 최종적으로 몇 % 찰까"에 대한 답입니다.

**알고리즘 — 픽업6**, 한 문장:

> **"최근 여섯 달은 이맘때부터 월말까지 평균 몇 %p 더 채워졌나 — 그만큼을 지금 점유율에 더한다."**

```
예상 점유율 = 현재 OTB + mean( 최종점유율_h − 같은시점OTB_h )

  h = 대상월 직전 6개 달력월 중 "완료됐고 예약 ≥3건"인 달
  클램프: [현재 OTB, 100%] + 진행 중인 달은 물리적 상한
```

**현재 정확도: 평균 오차 8.0%p** (2026-09 교체 전 구방식은 12.8%p — 모든 시점 구간에서 개선).

---

## 2. 알고리즘 이해하기 (처음 보는 사람용)

### 용어 4개

| 용어 | 뜻 | 예시 |
|---|---|---|
| **점유율** | 판매된 박 ÷ 판매 가능한 박 | 30일 중 23박 팔림 → 77% |
| **OTB** (On The Books) | **지금까지 확정된** 예약만으로 계산한 점유율 | 10월이 아직 안 왔는데 이미 74% |
| **잔여픽업** | 어떤 시점의 OTB에서 월말 최종까지 추가로 채워진 폭 | 이맘때 60% → 최종 93%면 +33%p |
| **D** | 그 달 시작까지 남은 일수 | 9월 6일에 10월을 보면 D=25 |

### 실제 예시 (2026-09-06에 10월을 예측, D-25)

| 히스토리 달 | D-25 시점 OTB | 최종 | 잔여픽업 |
|---|---|---|---|
| 2026-04 | 57% | 93% | +36 |
| 2026-05 | 68% | 100% | +32 |
| 2026-06 | 60% | 93% | +33 |
| 2026-07 | 65% | 90% | +25 |
| 2026-08 | 65% | 100% | +35 |
| (2026-09) | — 아직 미완료라 제외 | | |
| **평균** | | | **+32** |

→ 10월 현재 OTB 74% + 32%p = 106% → 상한 클램프 → **100%**

차트의 ⓘ 팝오버(`ForecastExplainer.tsx`)가 사용자에게 바로 이 계산을 보여줍니다.
**공식을 바꾸면 팝오버 문구도 함께 바꿔야 합니다.**

### 클램프 세 개 (상식의 코드화)

1. **바닥**: 예상치 ≥ 현재 OTB (확정 예약은 사라지지 않음)
2. **천장**: ≤ 100%
3. **물리적 상한** (진행 중인 달만): 이미 지나간 공실은 복구 불가 →
   `(지나간 날 중 점유된 박 + 남은 일수) ÷ 월 일수`

### 세부 규칙

- **"같은 시점 OTB" 재현**: 히스토리 달의 시작 D일 전(진행 중이면 경과 e일 후)까지
  **접수된(bookingDate)** 예약만으로 그때의 점유율을 다시 계산.
  접수일이 없거나 체크인보다 늦은 이상치는 체크인일로 간주(안전한 하한).
- **윈도는 "대상월 직전 6개 달력월 ∩ 완료된 달"** — 먼 미래 달일수록 표본이 3~4개로
  줄지만, "무조건 최근 완료 6개달"보다 먼 지평(D-60~120)에서 명확히 정확했습니다
  (백테스트 MAE 22.1 vs 17.7%p). **바꾸지 말 것.** 윈도 크기 4~10도 전부 시험했고 6이 최적(5~7은 근소 차).
- **폴백** (완료된 히스토리가 0개인 오픈 직후): `OTB + 최근평균최종 × (1 − 진행률)`.
- **STLY(작년 실적)·편향보정·동적가중은 쓰지 않습니다** — 전부 백테스트에서 기각(5장 표).

### 신뢰도 표시

`신뢰도 = 0.4 × min(1, 히스토리달수/4) + 0.6 × exp(−D/τ)`, D>120이면 추가 감쇠.
근거: 리드타임 실측(중앙값 31일, D-90 이전 접수 17%) — 먼 달은 "안 팔린" 게 아니라
"팔릴 시기가 안 온" 것. 신뢰도 30% 미만이면 툴팁에 "참고용" 표시.
τ(예약 도착 속도, 자동 추정)는 신뢰도 표시에만 쓰입니다.

---

## 3. 정확도 현황 (2026-09-06, 완료 15개 달 · 실제 훅 백테스트)

| 시점 | MAE (구방식→픽업6) | 편향 |
|---|---|---|
| D-90 | 27.2 → **17.0%p** | -11.1 |
| D-60 | 25.8 → **15.5%p** | -9.6 |
| D-30 | 15.1 → **9.7%p** | -5.7 |
| D-7 | 13.9 → **8.2%p** | -4.2 |
| 1일차 | 13.3 → **9.3%p** | +2.5 |
| 10일차 | 10.2 → **6.6%p** | +1.5 |
| 20일차 | 5.5 → **2.8%p** | +0.1 |
| **전체(174개 시점)** | 12.8 → **8.0%p** | -2.4 |

일단위 전체 스윕(2,242개 시점)도 18.5 → 12.2%p.
히스토리가 갖춰진 2026-03 이후 완료달들은 **월평균 오차 2.4~7.7%p**.

### 알려진 한계

1. **위 수치의 오차 대부분은 오픈 초기(2025-06~11)의 것** — 참조할 과거가 없던 시절이라
   지금의 예측 품질과 무관. 데이터가 쌓일수록 전체 MAE는 내려가는 게 정상.
2. **거의 만실인 달을 100%로 예측하는 경향** — 이 숙소는 실제로 만실이 잦아 오류가
   아니며, 빗나가도 3~10%p 이내.
3. **겨울 달의 먼 지평 예측** — 픽업6은 계절을 모릅니다. 여름 픽업으로 12~1월을 멀리서
   예측하면 과대 소지 (신뢰도 표시가 보완, 근본 해법은 5장 로드맵).

---

## 4. 매월 할 일 (운영 루틴)

### 4-1. 데이터 내려받기 → 합치기

Supabase SQL Editor에서 (100줄씩 잘리므로 OFFSET 0, 100, 200…으로 반복):

```sql
SELECT guestname, checkin, checkout, bookingdate,
       amount, commission, channel, status, property_id
FROM bookings
WHERE status IN ('confirmed','checked in','completed')
ORDER BY checkin LIMIT 100 OFFSET 0;
```

각 결과를 CSV로 받아 프로젝트 폴더에 놓고:

```bash
node -e "
const fs=require('fs');let all=[],head=null;
for(const i of [1,2,3,4]){
  const t=fs.readFileSync('Supabase Snippet Untitled query ('+i+').csv','utf8').replace(/^﻿/,'').trim();
  const L=t.split(/\r?\n/); if(!head)head=L[0]; all=all.concat(L.slice(1));
}
fs.writeFileSync('bookings_latest.csv',head+'\n'+all.join('\n'));
console.log('합침:',all.length,'건');"
```

### 4-2. 점검 실행

```bash
node --import ./__loader-reg.mjs monthly_review.mjs
```

출력 3부분: ① 최근 달 시점별 예측 vs 실제 ② 전체 MAE·편향 ③ **자가개선 권고** —
설정 후보(윈도 4~9 × 평균/중앙값, 데이터가 쌓이면 STLY 블렌드 포함)를 워크포워드로
재평가해, 현행보다 **0.5%p 이상** 좋은 후보가 있을 때만 교체를 권고합니다.
권고가 나오면 `useDesktopStats.ts`에서 상수를 바꾸고 `backtest_hook.mjs`로 확정하세요.

### 4-3. 정확도 이력

| 점검일 | 완료 달 | 전체 MAE | 편향 | 비고 |
|---|---|---|---|---|
| 2026-09-06 | 15개 | 14.0%p | -4.2%p | 구방식 마지막 측정 |
| 2026-09-06 | 15개 | **8.7%p** | -2.8%p | 픽업6 교체 직후 |
| | | | | |

---

## 5. 발전 로드맵

### 계절성 (STLY 블렌드) — 최우선 후보

**언제부터 가능한가**: 데이터가 2025-06 시작이라, "작년 실적을 가진 완료달"이
아직 하나도 없습니다(그래서 이번 교체 때 STLY 항은 검증 불가로 제외).

| 시점 | 상태 |
|---|---|
| 2026-11-01 | 2026-10 완료 → 첫 STLY 쌍(2025-10 대비) 생성. 권고 모드가 자동으로 STLY 후보 평가 시작 |
| 2027-01~02 | 3~4쌍 확보 — 첫 유의미한 판정 가능. 특히 겨울 달이라 계절성 효과가 가장 잘 보이는 구간 |
| 2027-06 | 12쌍 — 확정적 판단 가능 |

**설계안**: 먼 지평에서만 섞는다 — `D > 60일 때: 예상 = (1−w)·픽업6 + w·작년최종`,
w는 0.3/0.5 후보. 가까운 지평은 픽업6이 이미 충분히 정확하므로 건드리지 않음.
**표본 3쌍 미만이거나 개선 폭 0.5%p 미만이면 넣지 않는다** (과적합 방지).

### 그 외 방향 (우선순위 순)

1. **하이퍼파라미터 자가선택 승격** — 지금은 monthly_review가 "권고"만 함(사람이 적용).
   몇 달 관찰해 권고가 안정적이면 훅 안에서 자동 선택으로 승격 검토.
2. **앙상블(전문가 가중)** — 픽업6/STLY블렌드/최근평균을 최근 오차 역수로 가중.
   데이터 24개월+ 시점에 검토.
3. **편향 미세보정 재도입** — 구방식의 실패 원인(공식 복사본 불일치)은 사라졌지만,
   표본 30개월+ 전에는 노이즈 학습 위험이 커 비권장.

### 기각된 후보 (재시도 전에 읽을 것 — 2026-09 백테스트, 실데이터 301건)

| 후보 | 결과 |
|---|---|
| 곱셈 픽업 (OTB ÷ 과거 비율) | OTB 작을 때 폭주, MAE 9.8%p |
| 중앙값/절사평균/지수감쇠/선형가중 집계 | 평균과 동률이거나 열세 |
| STLY·최근평균 앵커 블렌드 (전 구간) | 개선 없음 |
| OTB 편차 보정 β | 무가치한 복잡도 |
| headroom `(100−OTB)/100` | 일관된 과소예측 (구방식에서도 기각) |
| "최근 완료 6개달" 윈도 | 먼 지평 크게 악화 (22.1 vs 17.7%p) |
| 램프업 달 히스토리 제외 | 크게 열세 (초기엔 그것뿐이라) |

---

## 6. 검증 도구 — 원칙: 최종 판단은 반드시 실제 훅으로

세 스크립트 모두 `useDesktopStats.ts`를 **직접 import**하고 React/스토어만 가짜로
채우므로 화면 값과 반드시 일치합니다. 과거에 공식을 재현한 별도 스크립트로 검증하다
오진해 두 번 잘못 고친 전례가 있습니다. 후보 탐색용 프로토타입은 괜찮지만,
**채택 판단과 최종 수치는 훅 기반 스크립트로** 하세요. 수정은 한 번에 하나씩 + 전후 비교.

| 스크립트 | 용도 |
|---|---|
| `run_hook_check.mjs` | 지금 화면에 나올 값 확인 |
| `backtest_hook.mjs` | D-90~30일차 전 구간 정확도 (수정 전후 비교) |
| `monthly_review.mjs` | 월말 정기 점검 + 자가개선 권고 |

실행: `node --import ./__loader-reg.mjs <스크립트>`

<details>
<summary>보조 파일 재생성 (gitignore 대상 — 없을 때만)</summary>

```bash
cat > __loader.mjs <<'EOF'
export async function resolve(specifier, context, next) {
  if (specifier === 'react') return { url: new URL('./__fake_react.mjs', import.meta.url).href, shortCircuit: true };
  if (specifier.endsWith('store/useStore')) return { url: new URL('./__fake_store.mjs', import.meta.url).href, shortCircuit: true };
  if (specifier.endsWith('utils/colors')) return { url: new URL('./__fake_colors.mjs', import.meta.url).href, shortCircuit: true };
  return next(specifier, context);
}
EOF
cat > __loader-reg.mjs <<'EOF'
import { register } from 'node:module';
register('./__loader.mjs', import.meta.url);
EOF
cat > __fake_react.mjs <<'EOF'
export const useMemo = (fn) => fn();
EOF
cat > __fake_store.mjs <<'EOF'
export const useStore = () => globalThis.__STORE__;
useStore.getState = () => globalThis.__STORE__;
EOF
cat > __fake_colors.mjs <<'EOF'
export const getNatColor = () => '#888888';
EOF
```
</details>

---

## 7. 데이터 품질 체크 (예측 정확도에 직결)

**① iCal "Not available" 중 금액 0원 장기 블록** = 판매 차단이지 예약이 아님 → **삭제**.
(과거 182박짜리가 그 기간 점유율을 전부 100%로 만든 사례 있음. 검증 스크립트는
자동 제외하지만 **앱 화면은 제외하지 않으므로** 발견 즉시 지울 것.)

**② 접수일 > 체크인 이상치** — 픽업6이 체크인일로 클램프해 방어하지만 원본 수정 권장.

```sql
-- ①: 의심 블록
SELECT guestname, checkin, checkout, amount, (checkout::date - checkin::date) AS 박수
FROM bookings WHERE guestname = 'Not available' ORDER BY 박수 DESC;
-- ②: 접수일 이상치
SELECT guestname, checkin, bookingdate FROM bookings
WHERE bookingdate > checkin OR bookingdate > CURRENT_DATE;
```

---

## 8. 역사 (한 단락)

2026-09까지는 "페이스 예측 + STLY 동적가중 + 편향보정 커브"(약 290줄, 공식이 본체와
편향 루프에 이중 복사)를 썼으나, 달 시작 전 과소(-21%p)·시작 후 과대(+11%p) 반전과
장치 간 상쇄 문제가 반복됐습니다. 2026-09-06 실데이터 301건으로 후보 25종을
워크포워드 백테스트해 픽업6으로 전면 교체(MAE 12.8→8.0%p), 공식은 한 곳으로
줄었습니다. 실험 전 기록과 구방식 문서는 git 히스토리(커밋 `e458944` 및 그 이전의
FORECAST_REBUILD_2026-09.md, FORECAST_SYSTEM.md, FORECAST_DIAGNOSIS.md,
FORECAST_ALGORITHM.md)에 있습니다.
